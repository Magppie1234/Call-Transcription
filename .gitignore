# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
.zoho_token_cache.json

# Runtime junk
*.log
*.pid
.DS_Store

# Discovery output (may contain customer data)
out/
recordings/
*.mp3
*.wav

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# dashboard-v2 (Vite build + real customer dataset — regenerate locally with
# scripts/build_ci_dataset.py rather than committing 722 real names/transcripts)
dashboard-v2/node_modules/
dashboard-v2/dist/
dashboard-v2/dist-ssr/
dashboard-v2/src/data/real/
